# 0.15.1

example 读取源码时，请求超过 100 行会自动按页返回，通过 `nextLine` 继续读取；不再因模型请求 120 行等有效数量而产生失败提示。每页大小、索引范围和参数类型校验仍保留。

共用历史栏在销毁时同时关闭重命名、删除和导出窗口，防止退出或权限变化后残留内容。宿主兼容文档补充 Cordis 注入作用域与新建对话的验收要求。

官方宿主仍锁定 `0.1.5-alpha.2`，无需再次迁移 Session 日志。
