# DSH Plugin Manager 0.13.1

部署终端每一步最右侧显示 `耗时 HH:MM:SS.s`，执行中每 100 毫秒刷新，成功或失败后保留实际处理耗时，排除补满进度条的动画时间。重定向输出保留开始及最终耗时，窄终端优先保留耗时字段。

部署文档补充了全量备份范围、归档保存位置、压缩及校验开销，以及停服期间执行备份的行为。备份范围和恢复流程不变；等待百分比不代表实际数据进度。

沿用统一框架版本规则，manager、kit、auth、example 同步至 0.13.1；定制插件版本不变。计时测试在 Windows 通过 6 项、跳过 1 项 POSIX 信号测试；Linux 容器 7 项全部通过。未执行生产部署，未测量用户服务器的备份速度。
