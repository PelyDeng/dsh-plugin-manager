{
  "name": "dsh-plugin-manager-workspace",
  "version": "0.14.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "version:sync": "node scripts/version.mjs sync",
    "version:check": "node scripts/version.mjs check",
    "version:set": "node scripts/version.mjs set",
    "build": "node scripts/workspace.mjs build",
    "check": "node scripts/workspace.mjs check",
    "list:plugins": "node scripts/run-plugin-task.mjs list",
    "clean": "node scripts/clean.mjs",
    "package": "node scripts/package-plugins.mjs",
    "test": "pnpm build && pnpm -r --workspace-concurrency=1 --if-present test"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "4.0.2",
    "@types/node": "^22.20.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "tsdown": "^0.22.2"
  }
}
